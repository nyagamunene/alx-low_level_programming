#include "main.h"
/**********************
	INCOMPLETE
***********************/





/**
*
*
*
*/
char **strtow(char *str)
{
	char **Array;
	int len, i, count = 0;

	if (str == NULL || str == "")
	{
		return (NULL);
	}
	for (len = 0; str[len] != "\0"; len++)
	{
		if (str[len] == 32)
		{
			Array[count] = ;
		}
	}
}
